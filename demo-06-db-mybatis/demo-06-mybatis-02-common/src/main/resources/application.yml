spring:
  profiles:
    active: oracle
  datasource:
    driver-class-name: ${db.driver}
    url: ${db.url}
    username: ${db.username}
    password: ${db.password}



# 打印SQL 到日志
logging:
  level:
    com.suyh.mapper: debug
